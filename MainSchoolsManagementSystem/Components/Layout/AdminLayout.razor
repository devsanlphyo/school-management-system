@inherits LayoutComponentBase
@inject AuthenticationStateProvider AuthStateProvider
@inject IDbContextFactory<MainSchoolsManagementSystem.Data.ApplicationDbContext> DbContextFactory
@inject NavigationManager NavigationManager
@using Microsoft.EntityFrameworkCore
@using System.Security.Claims

<div class="admin-layout">
    <!-- Sidebar Overlay (Mobile only) -->
    <div class="sidebar-overlay @(isSidebarOpen ? "show" : "")" @onclick="ToggleSidebar"></div>

    <!-- Sidebar -->
    <aside class="admin-sidebar @(isSidebarOpen ? "open" : "")">
        <MainSchoolsManagementSystem.Components.Shared.BrandLogo Role="Admin" />
        <nav class="sidebar-nav">
            <NavLink class="nav-item" href="admin/schools" @onclick="CloseSidebar">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 10v6M2 10l10-5 10 5-10 5z"></path><path d="M6 12v5c0 2 2 3 6 3s6-1 6-3v-5"></path></svg>
                Schools
            </NavLink>
            <NavLink class="nav-item" href="admin/users" @onclick="CloseSidebar">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg>
                Users
            </NavLink>
            <NavLink class="nav-item" href="admin/devices" @onclick="CloseSidebar">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect><line x1="12" y1="18" x2="12.01" y2="18"></line></svg>
                Devices
            </NavLink>
            <NavLink class="nav-item" href="admin/profile" @onclick="CloseSidebar">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>
                Profile
            </NavLink>
            <NavLink class="nav-item" href="admin/settings" @onclick="CloseSidebar">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>
                Settings
            </NavLink>
        </nav>
        <div class="sidebar-footer">
            <button type="button" class="btn-premium btn-secondary-custom" style="width: 100%;" @onclick="ShowLogoutConfirm">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 0.5rem;"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
                Logout
            </button>
        </div>
    </aside>

    <!-- Main Panel -->
    <main class="admin-main">
        <MainSchoolsManagementSystem.Components.Shared.PortalHeader 
            Title="Admin Portal" 
            ShowSchoolIcon="false" 
            UserDisplayName="@userDisplayName" 
            UserProfilePicturePath="@userProfilePicturePath"
            OnToggleSidebar="ToggleSidebar" />

        <div class="admin-content">
            @Body
        </div>
    </main>

    @if (showLogoutConfirm)
    {
        <div class="confirm-modal-backdrop" @onclick="CancelLogout">
            <div class="confirm-modal-card" @onclick:stopPropagation>
                <div class="confirm-modal-header">
                    <h3>Confirm Logout</h3>
                </div>
                <div class="confirm-modal-body">
                    <p>Are you sure you want to log out?</p>
                </div>
                <div class="confirm-modal-actions">
                    <button class="confirm-btn cancel" @onclick="CancelLogout">Cancel</button>
                    <form action="/Account/Logout" method="post" style="display:inline; margin:0;">
                        <AntiforgeryToken />
                        <button type="submit" class="confirm-btn delete" style="background-color: var(--danger);">Logout</button>
                    </form>
                </div>
            </div>
        </div>
    }
</div>

@code {
    private string userDisplayName = "System Admin";
    private string? userProfilePicturePath;
    private bool isSidebarOpen = false;
    private bool showLogoutConfirm = false;

    private void ToggleSidebar()
    {
        isSidebarOpen = !isSidebarOpen;
    }

    private void CloseSidebar()
    {
        isSidebarOpen = false;
    }

    private void ShowLogoutConfirm() => showLogoutConfirm = true;
    private void CancelLogout() => showLogoutConfirm = false;

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = authState.User;
        if (user.Identity?.IsAuthenticated == true)
        {
            userDisplayName = user.Identity.Name ?? "System Admin";
            
            using var db = await DbContextFactory.CreateDbContextAsync();
            var userId = user.FindFirst(ClaimTypes.NameIdentifier)?.Value;
            if (!string.IsNullOrEmpty(userId))
            {
                var appUser = await db.Users.FirstOrDefaultAsync(u => u.Id == userId);
                if (appUser != null)
                {
                    if (!string.IsNullOrWhiteSpace(appUser.FullName))
                    {
                        userDisplayName = appUser.FullName;
                    }
                    userProfilePicturePath = appUser.ProfilePicturePath;
                }
            }
        }
    }
}
