# Rules

- **Role Terminology & Routing**: Use `headmaster` instead of `admin` for school-level administration routes and UI titles (e.g., `/headmaster/...` instead of `/admin/...`).
- **Clarify Before Assuming**: Always ask the user for clarification before assuming role definitions, routing structures, or permissions. Do not implement based on assumptions without confirming.
